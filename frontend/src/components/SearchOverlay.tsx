import React from 'react';
import { mediaTypes } from '../hooks/useMedia';

interface SearchOverlayProps {
  media: mediaTypes;
}

const SearchOverlay = ({ media }: SearchOverlayProps) => {
  return <div></div>;
};

export default SearchOverlay;
